import Image from "next/image";

export default function HoverImage({ src, alt, fill=false }: { src: string, alt: string, fill?: boolean }) {
    return (
        <div className="relative w-full h-full">
            
        </div>
    )
}